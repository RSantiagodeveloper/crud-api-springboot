server:
  port: 8080

spring:
  application:
    name: backend-tamplate
  profiles:
    default: dev
  datasource:
    url: jdbc:postgresql://${DB_HOST}/${DB_NAME}?currentSchema=${DB_SCHEMA}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: create
    defer-datasource-initialization: true

logging:
  level:
    org.hibernate.SQL: ${SQL_LOGS_LEVEL} # log para componentes a nivel de framework hibernate
    com.musanlori.dev.crud.api: ${PROJECT_LOGS_LEVEL}

springdoc:
  swagger-ui:
    path: /componente-ui-docs
    enabled: true
  api-docs:
    path: /componente-api-docs
    enabled: true